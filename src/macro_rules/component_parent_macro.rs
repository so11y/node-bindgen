#[macro_export]
macro_rules! component_macro {
    () => {
        // fn get_constraint(&self) -> &Constraint {
        //     return self.constraint.as_ref().unwrap();
        // }

        // fn set_constraint(&mut self, constraint: Constraint) {
        //     self.constraint = Some(constraint);
        // }
    };
}
